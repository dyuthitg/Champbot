# Core runtime dependencies (API + agent runtime).
# Heavy ML/NLP libraries live in requirements-ml.txt so the API image and the
# default agent runtime install fast and don't need Torch/transformers.
# Install ML extras with:  pip install -r requirements-ml.txt

# --- Async / core ---
aiohttp>=3.9,<4
redis>=5.0,<6
python-dotenv>=1.0,<2
uvloop>=0.19,<0.20; sys_platform != "win32"
tenacity>=8.2,<9

# --- Web framework / API ---
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.35
gunicorn>=21.2,<23
websockets>=12.0,<13
python-multipart>=0.0.9,<0.1

# --- Data validation ---
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
email-validator>=2.1,<3

# --- LinkedIn automation (Playwright fallback transport) ---
playwright>=1.42,<2
beautifulsoup4>=4.12,<5
requests>=2.31,<3
# Native mobile-API transport (primary): TLS-fingerprinted HTTP client
curl_cffi>=0.6,<0.8
# Selenium is still imported by the legacy account_manager/interaction agents.
# These become optional once those imports are made lazy in the mobile-transport phase.
selenium>=4.19,<5
undetected-chromedriver>=3.5,<4

# --- LLM (all inference routed through OpenRouter) ---
openai>=1.30,<2          # used as the OpenRouter-compatible client
httpx>=0.27,<0.28

# --- Database ---
sqlalchemy[asyncio]>=2.0.29,<2.1
alembic>=1.13,<2
asyncpg>=0.29,<0.31
psycopg2-binary>=2.9,<3  # sync Postgres driver: scripts/migrate.py inspects the
                         # schema with a sync engine before uvicorn starts
aiosqlite>=0.20,<0.21    # SQLite async driver for local/test runs

# --- Auth (Clerk) ---
pyjwt[crypto]>=2.8,<3    # verify Clerk session JWTs against JWKS

# --- Security ---
cryptography>=42.0,<44   # Fernet encryption of account credentials

# --- Monitoring / logging ---
prometheus-client>=0.20,<0.22
structlog>=24.1,<25

# --- Utilities ---
python-dateutil>=2.9,<3
pytz>=2024.1
click>=8.1,<9
rich>=13.7,<14
pyyaml>=6.0,<7
orjson>=3.9,<4

# --- Testing ---
pytest>=8.1,<9
pytest-asyncio>=0.23,<0.25
pytest-cov>=5.0,<6
pytest-mock>=3.12,<4
fakeredis>=2.21,<3
faker>=24.0,<25
